require "spec_helper"

describe TicTacToe do
  it "has a version number" do
    expect(TicTacToe::VERSION).not_to be nil
  end

  it "does something useful" do
    expect('deine mudda').to eq('deine mudda')
  end
end
